[package]
name = "mouse-shader"
version = "0.0.0"
publish = false
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

# Naga does not support the FloatControls2 capability used by the default math mode.
[features]
compat-math = []

[dependencies]
shared = { path = "../../shaders/shared" }
spirv-std = { workspace = true }

[package.metadata.release]
release = false
